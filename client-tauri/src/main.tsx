import React,{useEffect,useState}from"react";import{createRoot}from"react-dom/client";import"./style.css";
const API="http://127.0.0.1:8080";
function App(){const[data,setData]=useState<any[]>([]);const[detail,setDetail]=useState<any>(null);useEffect(()=>{fetch(API+"/api/v1/feed?user_id=1&country=US&language=en&page_size=20").then(r=>r.json()).then(j=>setData(j.items||[]))},[]);return <main><header><h1>Short Drama</h1><span>US · English</span></header><section className="feed">{data.map(x=><article key={x.drama_id} onClick={()=>fetch(API+"/api/v1/dramas/"+x.drama_id).then(r=>r.json()).then(j=>setDetail(j.drama))}><div className="cover">{x.cover?<img src={x.cover}/>:<span>DRAMA</span>}</div><h2>{x.title}</h2><small>{x.reasons?.join(" · ")}</small></article>)}</section>{detail&&<div className="modal"><div><button onClick={()=>setDetail(null)}>关闭</button><h1>{detail.title}</h1><p>{detail.description}</p><p>{detail.is_paid?"付费短剧":"免费观看"}</p><button onClick={()=>alert(detail.is_paid?"进入购买流程":"开始播放")}>{detail.is_paid?"购买观看":"开始观看"}</button></div></div>}</main>}createRoot(document.getElementById("root")!).render(<App/>);