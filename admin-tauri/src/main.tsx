import React,{useState}from"react";import{createRoot}from"react-dom/client";import"./style.css";
const API="http://127.0.0.1:8081";
function App(){const[login,setLogin]=useState(false);const[items,setItems]=useState<any[]>([]);const[form,setForm]=useState<any>({title:"",description:"",country:"US",language:"en",total_episodes:1,is_paid:false});const load=async()=>{const r=await fetch(API+"/api/v1/admin/dramas?page=1&page_size=100");const j=await r.json();setItems(j.items||[])};if(!login)return <main className="login"><h1>Short Drama Admin</h1><input placeholder="账号"/><input placeholder="密码" type="password"/><button onClick={()=>setLogin(true)}>登录</button></main>;return <main><header><h1>短剧后台</h1><button onClick={()=>setLogin(false)}>退出</button></header><section className="grid"><div className="card"><h2>发布短剧</h2><input placeholder="标题" value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/><input placeholder="简介" value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/><input placeholder="国家" value={form.country} onChange={e=>setForm({...form,country:e.target.value})}/><input placeholder="语言" value={form.language} onChange={e=>setForm({...form,language:e.target.value})}/><input type="number" placeholder="集数" value={form.total_episodes} onChange={e=>setForm({...form,total_episodes:Number(e.target.value)})}/><label><input type="checkbox" checked={form.is_paid} onChange={e=>setForm({...form,is_paid:e.target.checked})}/> 付费短剧</label><button onClick={async()=>{await fetch(API+"/api/v1/admin/dramas",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});load()}}>创建短剧</button></div><div className="card"><h2>短剧管理</h2><button onClick={load}>刷新</button><table><thead><tr><th>ID</th><th>标题</th><th>付费</th><th>状态</th><th>操作</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{x.id}</td><td>{x.title}</td><td>{x.is_paid?"付费":"免费"}</td><td>{x.status}</td><td><button onClick={async()=>{await fetch(API+"/api/v1/admin/dramas/"+x.id+"/status",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({status:x.status===1?2:1})});load()}}>上下架</button></td></tr>)}</tbody></table></div></section></main>}createRoot(document.getElementById("root")!).render(<App/>);